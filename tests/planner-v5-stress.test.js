const fs=require('fs'),vm=require('vm'),assert=require('assert');
let writes=0;const listeners={};const ctx={console,Date,JSON,Math,setTimeout:()=>0,clearTimeout:()=>{},CustomEvent:function(type,init){this.type=type;this.detail=init?.detail},localStorage:{getItem(){return null},setItem(){writes++}},state:{completed:{},xpLedger:{},bonusLedger:{},activityDates:[],customQuests:[],overrides:{},questPlans:{},filters:{domain:'Todos'}},DOMAIN_META:{Pessoal:{},GSA:{},Estudos:{},Carreira:{}},QUEST_SEED:[]};ctx.globalThis=ctx;ctx.window=ctx;ctx.addEventListener=(t,f)=>(listeners[t]||(listeners[t]=[])).push(f);ctx.dispatchEvent=e=>{for(const f of listeners[e.type]||[])f(e)};ctx.iso=d=>{const x=new Date(d||Date.now());x.setMinutes(x.getMinutes()-x.getTimezoneOffset());return x.toISOString().slice(0,10)};ctx.dfrom=s=>new Date(`${s}T12:00:00`);ctx.today=()=> '2026-08-11';ctx.addDays=(s,n)=>{const d=ctx.dfrom(s);d.setDate(d.getDate()+n);return ctx.iso(d)};ctx.diffDays=(a,b)=>Math.round((ctx.dfrom(b)-ctx.dfrom(a))/864e5);ctx.weekStart=s=>{const d=ctx.dfrom(s),n=(d.getDay()+6)%7;d.setDate(d.getDate()-n);return ctx.iso(d)};ctx.saveState=()=>{writes++;ctx.dispatchEvent(new ctx.CustomEvent('my-performance-state-saved',{detail:ctx.state}))};ctx.Q=o=>ctx.QUEST_SEED.push(Object.assign({domain:'Pessoal',questType:'side',cadence:'once',weekdays:[],xp:20,difficulty:1},o));
for(let i=0;i<180;i++)ctx.Q({id:`stress-main-${i}`,title:`Stress ${i}`,domain:i%3===0?'Estudos':'GSA',category:i%4===0?'EVA':'Editais',questType:'main',cadence:'once',startDate:'2026-08-10',dueDate:ctx.addDays('2026-08-11',5+(i%100)),durationMin:45+(i%4)*30,priorityLevel:i%13===0?'critical':i%5===0?'high':'normal',difficulty:i%4+1});
[
{id:'personal-wake',title:'Acordar',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4,5,6],durationMin:10},{id:'routine-water-am',title:'Água',domain:'Pessoal',cadence:'daily',weekdays:[1,2,4,5,6],durationMin:5},{id:'routine-hygiene-am',title:'Higiene',domain:'Pessoal',cadence:'daily',weekdays:[1,2,4,5,6],durationMin:10},{id:'personal-breakfast',title:'Café',domain:'Pessoal',cadence:'daily',weekdays:[1,2,4,5,6],durationMin:30},{id:'personal-gym',title:'Academia',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4,5,6],durationMin:90},{id:'routine-shower-post-gym',title:'Banho',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4,5,6],durationMin:30},{id:'personal-lunch',title:'Almoço',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4,5,6],durationMin:30},{id:'routine-dinner',title:'Jantar',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4],durationMin:30},{id:'routine-hygiene-night',title:'Higiene sono',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4],durationMin:15},{id:'personal-sleep',title:'Dormir',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4],durationMin:10},{id:'personal-evening-activity',title:'Atividade pessoal',domain:'Pessoal',cadence:'daily',weekdays:[1,2,3,4],durationMin:30}
].forEach(ctx.Q);
ctx.quests=()=>ctx.QUEST_SEED.concat(ctx.state.customQuests||[]).map(q=>Object.assign({},q,ctx.state.overrides[q.id]||{})).filter(q=>!q.disabled);ctx.questById=id=>ctx.quests().find(q=>q.id===id);ctx.scheduled=(q,date)=>{const wd=ctx.dfrom(date).getDay();if(q.cadence==='daily'||q.cadence==='weekly')return !(q.weekdays||[]).length||(q.weekdays||[]).includes(wd);if(q.cadence==='monthly')return true;if(q.cadence==='once')return date>=(q.startDate||date)&&date<=(q.dueDate||date);return false};ctx.done=()=>false;ctx.hasEverDone=()=>false;
vm.createContext(ctx);for(const f of ['calendar-model-v3.js','calendar-model-fastpath-v4.js','calendar-domain-v5.js','planner-engine-v5.js'])vm.runInContext(fs.readFileSync(f,'utf8'),ctx);const E=ctx.MyPerformancePlannerEngine;assert(E&&E.VERSION===5);const startupWrites=writes;for(let i=0;i<20;i++){E.planDay('2026-08-11');E.missionNow('2026-08-11',new Date('2026-08-11T10:00:00'));E.diagnostics('2026-08-11');E.strategicWeek('2026-08-11')}for(let i=0;i<14;i++)E.planDay(ctx.addDays('2026-08-11',i));const m=E.metrics();assert(m.rangeCacheEntries<=2,`range cache too large ${m.rangeCacheEntries}`);assert(m.dayCapacityCacheEntries<=500,`day capacity cache too large ${m.dayCapacityCacheEntries}`);assert.strictEqual(writes,startupWrites,`read-only planning wrote localStorage: ${startupWrites}->${writes}`);const tue=E.planDay('2026-08-11');assert(tue.slots.some(x=>x.eventId==='event-therapy'&&x.eventSlot));for(let i=1;i<tue.slots.length;i++)assert(tue.slots[i-1].end<=tue.slots[i].start,'overlap under stress');console.log('Planner V5 bounded-memory/zero-write stress regression passed',{...m,startupWrites,writes});
