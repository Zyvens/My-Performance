const fs=require('fs'),vm=require('vm'),assert=require('assert');
const code=fs.readFileSync('daily-review-v6.js','utf8');
const storage={m:new Map([['my_performance_daily_review_v6','legacy'],['my_performance_last_live_date','2026-08-11']]),getItem(k){return this.m.get(k)||null},setItem(k,v){this.m.set(k,String(v))},removeItem(k){this.m.delete(k)}};
let invalidations=0,renders=0;const state={plannerDate:'2026-08-11',view:'today',dailyReviewV6:{pending:[{x:1}],history:[{x:1}]}};const E={invalidate(){invalidations++}};const listeners={};const document={visibilityState:'visible',addEventListener(t,f){listeners[t]=f}};const window={MyPerformanceClock:{today:()=> '2026-08-12'},MyPerformancePlannerEngine:E,addEventListener(){}};const ctx={console,state,localStorage:storage,window,document,render(){renders++}};ctx.globalThis=ctx;vm.createContext(ctx);vm.runInContext(code,ctx);
assert.strictEqual(state.plannerDate,'2026-08-12');assert(!state.dailyReviewV6,'past-review state must be removed');assert.strictEqual(storage.getItem('my_performance_daily_review_v6'),null);assert.strictEqual(storage.getItem('my_performance_last_live_date'),'2026-08-12');assert.strictEqual(invalidations,1,'date rollover must invalidate the hidden plan');assert.strictEqual(window.MyPerformanceDailyReview.reviewRequired(),false);assert.strictEqual(window.MyPerformanceDailyReview.pending().length,0);assert.strictEqual(window.MyPerformanceDailyReview.finalize(),true);
console.log('Daily rollover V13 zero-history regression passed');
